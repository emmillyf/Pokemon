import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBarStyle: {
    backgroundColor: "#fff",
    height: 70,
    justifyContent: "center",
    alignItems: "center",
  },
  tabBarItemStyle: {
    justifyContent: "center",
    alignItems: "center",
    paddingTop: 15,
  },
  containerStyle: {
    justifyContent: "center",
    alignItems: "center",
  },
});
